# Valve / GPIO
# BCM pin number driving the relay/solenoid (default: 17 = physical pin 11)
VALVE_GPIO_PIN=17

# true  = relay activates on HIGH (default)
# false = relay activates on LOW (common for cheap relay modules)
VALVE_ACTIVE_HIGH=true
